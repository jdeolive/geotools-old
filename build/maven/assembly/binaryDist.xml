<?xml version="1.0" encoding="UTF-8"?>
<!-- ======================================================================
       Assembly descriptor for binaries. This descriptor is a modified
       copy of the Maven "bin" predefined descriptor published there:

       http://maven.apache.org/plugins/maven-assembly-plugin/descriptor-refs.html#bin

       It will creates a "gt2-${project.version}-bin.zip" file.  This file
       will contains a "gt2-${project.version}" root directory, which will
       contains every JAR copied by our custom "jar-collector" plugin.

       $Id$
       $URL$
     ====================================================================== -->

<assembly>
  <id>bin</id>
  <formats>
    <format>zip</format>
  </formats>

  <!-- Specifies which groups of files to include in the assembly. -->
  <fileSets>
    <fileSet>
      <includes>
        <include>README*</include>
      </includes>
    </fileSet>
    <fileSet>
      <outputDirectory></outputDirectory>
      <directory>licenses</directory>
      <includes>
        <include>*</include>
      </includes>
    </fileSet>
    <fileSet>
      <!-- The relative location from the module's directory. -->
      <directory>target/binaries</directory>

      <!-- The output directory relative to the root target.
           This is needed in order to prevent the assembly to store the
           JARs in a "target/binaries" directory inside the ZIP file. -->
      <outputDirectory></outputDirectory>
      <includes>
        <include>*.jar</include>
      </includes>
    </fileSet>
  </fileSets>
</assembly>
